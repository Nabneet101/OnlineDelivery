<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlinedelivery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/deliveryimg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/deliveryimg"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineDelivery">
        <activity android:name=".splashscreen">   <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".Animation" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>